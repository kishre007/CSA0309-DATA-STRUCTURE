#include <stdio.h>
int a[20][20],vis[20],n;
void dfs(int v){int i;
    printf("%d ",v);vis[v]=1;
    for(i=0;i<n;i++) if(a[v][i] && !vis[i]) dfs(i);
}
int main(){int i,j;
    scanf("%d",&n);
    for(i=0;i<n;i++)for(j=0;j<n;j++)scanf("%d",&a[i][j]);
    for(i=0;i<n;i++)vis[i]=0;
    printf("DFS: ");dfs(0);
}
